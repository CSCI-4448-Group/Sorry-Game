package com.project.sorryapp;

public class TwoCard extends Card
{
    public TwoCard()
    {
        set_card_value(2);
        set_description("Start a pawn out or moves one pawn forward 2 spaces. Whether you move or cannot move, draw again and move accordingly.");
    }
}
