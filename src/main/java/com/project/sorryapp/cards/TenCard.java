package com.project.sorryapp.cards;

public class TenCard extends Card
{
    public TenCard()
    {
        set_card_value(10);
        set_description("Moves one pawn forward 10 spaces or moves one pawn backwards 1 space.");
    }
}
